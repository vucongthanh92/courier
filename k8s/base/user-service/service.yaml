apiVersion: v1
kind: Service
metadata:
  name: user-service
spec:
  selector:
    app: user-service
  ports:
    - name: http
      port: 5000
      targetPort: 5000
    - name: grpc
      port: 5001
      targetPort: 5001
  type: ClusterIP
