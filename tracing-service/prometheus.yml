global:
  scrape_interval: 15s
scrape_configs:
  - job_name: courier-services
    static_configs:
      - targets: ['host.docker.internal:5001'] # ví dụ service exposed /metrics
        labels:
          service_name: user-service
          env: local
